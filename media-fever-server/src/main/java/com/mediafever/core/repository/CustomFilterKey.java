package com.mediafever.core.repository;

import com.jdroid.javaweb.search.FilterKey;

public enum CustomFilterKey implements FilterKey {
	
	NAME,
	USER_ID,
	USER_QUERY,
	USER_PUBLIC_PROFILE,
	WATCHED,
	IS_IN_WISHLIST,
	WATCHABLE_TYPES,
	IMAGE_REQUIRED,
	RATING_GREATER_THAN;
}
